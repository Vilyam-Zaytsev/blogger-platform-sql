export const titleConstraints = {
  minLength: 1,
  maxLength: 30,
};

export const shortDirectionConstraints = {
  minLength: 1,
  maxLength: 100,
};

export const contentConstraints = {
  minLength: 1,
  maxLength: 1000,
};
