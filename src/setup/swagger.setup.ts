import { INestApplication } from '@nestjs/common';
import { DocumentBuilder, SwaggerModule } from '@nestjs/swagger';
import { GLOBAL_PREFIX } from './global-prefix.setup';

export function swaggerSetup(app: INestApplication, isSwaggerEnabled: boolean) {
  if (!isSwaggerEnabled) {
    console.log('Swagger отключен в конфигурации');
    return;
  }

  const config = new DocumentBuilder()
    .setTitle('Blogger Platform API')
    .setDescription(
      `
      ## API для платформы блогеров

### 🌟 ОСНОВНЫЕ ВОЗМОЖНОСТИ:

**👤 Аккаунт и Профиль:**
- 🔐 Регистрация с подтверждением email
- 🔑 Вход через JWT Bearer Token (access + refresh)
- 🔄 Восстановление пароля
- 👥 Управление профилем
- 🍪 Сессии в httpOnly cookies

**📰 Платформа блогеров:**
- 📝 Создание и управление блогами
- ✍️ Публикация постов в блогах
- 💬 Система комментариев к постам
- 👍 Реакции на посты и комментарии (Like, Dislike, None)

**🎯 Викторины и игры:**
- 🎮 Прохождение викторин с автоматической проверкой ответов
- 🏆 Таблица лидеров и рейтинг игроков
- 📊 Статистика личных игр (общее количество игр, количество побед, количество поражений, количество игр сыгранных в ничью, средний балл)
- ⚙️ Админ управление викторинами и вопросами
- 📈 История пройденных викторин с подробными результатами

---

## 🔐 АУТЕНТИФИКАЦИЯ

### 1️⃣ JWT Bearer Token (для пользователей)

Используется для **всех пользовательских операций** (блоги, посты, комментарии, реакции, викторины).

**Когда использовать JWT Bearer Token:**
- GET **/api/auth/me** - получить профиль


- PUT **/api/comments/{commentId}** - обновить комментарий
- PUT **/api/comments/{commentId}/like-status** - обновить статус реакции на комментарий
- DELETE **/api/comments/{commentId}** - удалить комментарий
- GET **/api/comments/{id}** - получить комментарий по ID


- PUT /api/posts/{postId}/like-status - обновить статус реакции на пост
- POST /api/posts/{postId}/comments - создать комментарий к определенному посту
---

### 1️⃣ JWT Refresh Token (для пользователей)

Используется для **некоторых пользовательских операций** (обновление пары токенов, выход из системы, работа с девайсами с которых пользователь авторизовался).

**Когда использовать JWT Refresh Token:**
- POST **/api/auth/refresh-token** - обновление пары токенов
- POST **/api/auth/logout** - выход из системы (Refresh token инвалидируется, cookie очищается)


- GET **/api/security/devices** - получить список девайсов с активными сессиями
- DELETE **/api/security/devices** - завершить сессии на всех устройствах кроме текущего (с которого производится запрос)
- DELETE **/api/security/devices/{deviceId}** - завершить сессию на определенном устройстве



- PUT /api/posts/{postId}/like-status - обновить статус реакции на пост
- POST /api/posts/{postId}/comments - создать комментарий к определенному посту
---

### 2️⃣ Basic Auth (для администраторов и тестирования)

Используется для **админ операций** и **тестирования**



**Когда использовать Basic Auth:**
- GET **api/sa/blogs** - получить список всех блогов
- POST **/api/sa/blogs** - создать новый блог
- PUT **/api/sa/blogs/{id}** - обновить определенный блог
- DELETE **/api/sa/blogs/{id}** - удалить определенный блог
- POST **/api/sa/blogs/{blogId}/posts** - создать новый пост для определенного блога
- GET **/api/sa/blogs/{blogId}/posts** - получить список постов для определенного блога
- PUT **/api/sa/blogs/{blogId}/posts/{postId}** - обновить определенный пост принадлежащий определенному блогу
- DELETE **/api/sa/blogs/{blogId}/posts/{postId}** - удалить определенный пост принадлежащий определенному блогу



- GET **/api/sa/quiz/questions** - получить список всех вопросов для викторины
- POST **/api/sa/quiz/questions** - создать новый вопрос для викторины
- DELETE **/api/sa/quiz/questions/{id}** - удалить определенный вопрос для викторины
- PUT **/api/sa/quiz/questions/{id}** - обновить определенный вопрос для викторины
- PUT **/api/sa/quiz/questions/{id}/publish** - опубликовать.снять с публикации определенный вопрос для викторины


- GET **/api/sa/users** - получить список пользователей
- POST **/api/sa/users** - создать нового пользователя
- DELETE **/api/sa/users/{id}** - удалить определенного пользователя

---

### 3️⃣ Public Access (без авторизации)

Следующие операции доступны без авторизации:
- POST **/api/auth/registration** - зарегистрироваться в системе
- POST **/api/auth/registration-confirmation** - подтвердить регистрацию в системе
- POST **/api/auth/registration-email-resending** - запросить повторную отправку письма для подтверждения регистрации в системе
- POST **/api/auth/login** - авторизоваться в системе
- POST **/api/auth/password-recovery** - сделать запрос на смену пароля
- POST **/auth/new-password** - изменить пароль


- GET **/api/blogs** - получить список блогов
- GET **/api/comments/{id}** - получить определенный блог
- GET **/api/blogs/{blogId}/posts** - получить список постов определенного блога


- GET **/api/posts** - получить список постов
- GET **/api/posts/{id}** - получить определенный пост
- GET **/api/posts/{postId}/comments** - получить список комментариев к определенному посту


- GET **/api/comments/{id}** - получить определенный комментарий


- GET **/api/pair-game-quiz/users/top** - получить список ТОП игроков в викторину

---

## ⚠️ ОБРАБОТКА ОШИБОК

### Коды ответов:
- **200 OK** - успешный запрос
- **201 Created** - ресурс успешно создан
- **204 No Content** - успешный запрос без тела ответа
- **400 Bad Request** - ошибка валидации
- **401 Unauthorized** - не авторизован или неверные credentials
- **403 Forbidden** - авторизован, но нет прав
- **404 Not Found** - ресурс не найден
- **429 Too Many Requests** - превышен лимит запросов
- **500 Internal Server Error** - ошибка сервера
`,
    )
    .setVersion('1.0.0')
    .setTermsOfService('https://example.com/terms')
    .setContact('Blogger Platform Support', 'https://example.com', 'support@bloggerplatform.com')
    .addBearerAuth(
      {
        type: 'http',
        scheme: 'bearer',
        bearerFormat: 'JWT',
        name: 'JWT',
        description: 'JWT токен для пользовательских операций',
        in: 'header',
      },
      'Bearer',
    )
    .addCookieAuth(
      'refreshToken',
      {
        type: 'apiKey',
        in: 'cookie',
        name: 'refreshToken',
        description:
          'JWT Refresh Token (устанавливается автоматически при логине через httpOnly cookie)',
      },
      'JWT-refresh',
    )
    .addBasicAuth(
      {
        type: 'http',
        scheme: 'basic',
        name: 'Basic',
        description: 'Username:Password для админ/тестовых операций',
        in: 'header',
      },
      'Basic',
    )
    .addServer('http://localhost:3000', 'Локальная разработка')
    .addServer('https://api.example.com', 'Production')
    .addServer('https://staging.example.com', 'Staging')
    .build();

  const document = SwaggerModule.createDocument(app, config);
  SwaggerModule.setup(GLOBAL_PREFIX, app, document, {
    customSiteTitle: 'Blogger Platform API Documentation',
    swaggerOptions: {
      // Оставляем auth token в памяти браузера между обновлениями
      persistAuthorization: true,
      // Сортируем теги по алфавиту
      tagsSorter: 'alpha',
      // Сортируем операции по алфавиту внутри тега
      operationsSorter: 'alpha',
      // Показываем параметры запроса перед Response моделями
      docExpansion: 'list',
      // Фильтр для быстрого поиска операций
      filter: true,
      // Показываем Request/Response модели в примерах
      showRequestHeaders: true,
    },
    // Кастомизируем HTML страницу Swagger UI
    customCss: `
      .topbar { background-color: #1e3a5f; }
      .info { color: #333; }
      .scheme-container { background: #f5f5f5; }
    `,
  });
}
